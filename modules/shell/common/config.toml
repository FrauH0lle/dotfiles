# Packages
[[packages]]
install = [ "dotdeploy/shell-common" ]
if = "(eq DOD_DISTRIBUTION_NAME 'gentoo')"

[[packages]]
install = [ "dotdeploy-shell-common" ]
if = "(eq DOD_DISTRIBUTION_NAME 'ubuntu')"
